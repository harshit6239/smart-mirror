# Gesture Service - Optional Environment Variable Overrides
# 
# The service works out of the box with sensible defaults.
# Copy this file to ../.env ONLY if you need to override specific values.
#
# Default profile: Standard Windows/Linux settings (640x480, 30fps, debug on)
# Raspberry Pi profile: Use CONFIG_PROFILE=raspberry_pi (see below)
#
# All values below are OPTIONAL overrides.

# ==============================================================================
# PROFILE SELECTION (Optional)
# ==============================================================================
# Automatically loads optimized settings for your platform
# Options: 'default' (Windows/Linux) or 'raspberry_pi'
# CONFIG_PROFILE=raspberry_pi

# ==============================================================================
# CAMERA SETTINGS (Optional overrides)
# ==============================================================================
# Default profile: 640x480 @ 30fps
# Raspberry Pi profile: 320x240 @ 15fps
# CAMERA_DEVICE_ID=0
# CAMERA_WIDTH=640
# CAMERA_HEIGHT=480
# CAMERA_FPS=30

# ==============================================================================
# WEBSOCKET SETTINGS (Optional overrides)
# ==============================================================================
# Default: 0.0.0.0:5001
# WEBSOCKET_HOST=0.0.0.0
# WEBSOCKET_PORT=5001

# ==============================================================================
# GESTURE DETECTION SETTINGS (Optional overrides)
# ==============================================================================
# Default profile: 2 hands, 0.7 confidence
# Raspberry Pi profile: 1 hand (performance)
# GESTURE_MIN_DETECTION_CONFIDENCE=0.7
# GESTURE_MIN_TRACKING_CONFIDENCE=0.5
# GESTURE_MAX_NUM_HANDS=2
# GESTURE_COOLDOWN=0.5

# ==============================================================================
# DEBUG MODE (Optional override)
# ==============================================================================
# Default profile: true (shows camera feed)
# Raspberry Pi profile: false (headless)
# DEBUG=true

# ==============================================================================
# USAGE EXAMPLES
# ==============================================================================
#
# Run with default settings (no .env needed):
#   uv run main.py
#
# Run with Raspberry Pi profile:
#   CONFIG_PROFILE=raspberry_pi uv run main.py
#
# Override just the camera resolution:
#   Create .env with: CAMERA_WIDTH=1280 and CAMERA_HEIGHT=720
#
# Raspberry Pi profile + custom port:
#   Create .env with: CONFIG_PROFILE=raspberry_pi and WEBSOCKET_PORT=8080
#
