# Smart Mirror — Development Progress

> **Purpose:** Paste this file at the start of a new conversation to resume without losing context.
> Keep this updated as phases complete.

> **Widget authoring reference:** See [`docs/WIDGET_AUTHORING.md`](WIDGET_AUTHORING.md) for the full step-by-step guide on creating widgets, making them configurable, and registering them — includes annotated real examples (Clock, Quote, Weather).

---

## Current Status

**Active phase:** Phase 17 — Automation Service
**Last completed:** Phase 16 — Widget Registry (online)

---

## Phase Completion

| #   | Phase                                 | Status  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Config Foundation                     | ✅ Done | `electron-store`, `AppConfig` type, `config:get/set` IPC, `window.config` preload                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 1   | WiFi Provisioning                     | ✅ Done | `WifiService` (nmcli, stubs on non-Linux), `CompanionServer` on :8080, `SetupPage.tsx` with QR, `App.tsx` routing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2   | Page System                           | ✅ Done | `PageSystem`, `PageDots`, `useGesture` (arrow-key fallback), `useConfig`, default pages seeded                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 3   | Widget Grid Framework                 | ✅ Done | `WidgetGrid` (12×8 CSS grid), `WidgetWrapper` (ErrorBoundary + skeleton), `DummyWidget`, `widget-registry.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| —   | Cleanup                               | ✅ Done | Removed all user-written WS UI code (`ConnectionStatus`, `UI.tsx`, old IPC handlers)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 4   | Bundled Widgets: Clock + System Stats | ✅ Done | `ClockWidget` (setInterval, timezone/use24h config), `SystemStatsWidget` (polls `system:stats` IPC every 3 s, CPU+RAM+temp, graceful N/A on non-Pi), `window.system.getStats` preload, seeded on page 1                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 5   | Gesture ↔ WebSocket IPC              | ✅ Done | Full REST API + WebSocket at `/ws` (config-changed, page-change, notification events); `broadcastEvent` + `setCurrentPage`; `page:changed` IPC; `window.api.notifyPageChange` preload; `GestureWebSocketClient` wired in `main/index.ts` connecting to Python service on `ws://localhost:5001`                                                                                                                                                                                                                                                                                                                                                     |
| 6   | Companion Web App Shell               | ✅ Done | React SPA (`react-router-dom`, routes `/` `/settings` `/wifi`); served from `out/companion/` via `CompanionServer`; built with `companion.vite.config.ts`; `HomePage` polls status + WS gesture events; `SystemSettings` full settings form → `PUT /api/settings`; `WifiSetupPage`; mirror applies theme in real-time via `config.onChange`                                                                                                                                                                                                                                                                                                        |
| 7   | Layout Editor (Companion)             | ✅ Done | `react-grid-layout` v2 drag/resize editor; page tabs (add/rename/delete, double-click to rename); `findFreeSlot` — new widgets always placed in first free grid cell (no overlap); duplicate-widget-per-page guard; max 8 widgets/page, max 5 pages (enforced + UI disabled); 10-level undo stack (snapshot on drag/resize start + every mutation); toast notifications; `PUT /api/layouts` save → mirror re-renders live; `companion:url` IPC (`os.networkInterfaces`); `CompanionQR` overlay on mirror (bottom-right, tap to hide); `WIDGET_CATALOG` in companion lib                                                                            |
| 8   | Per-Widget Config UI                  | ✅ Done | `ConfigField` type in `widget-types.ts` + `widgets-catalog.ts`; `WidgetConfigForm` auto-renders inputs from schema (`string/number/boolean/select`, `secret` masking, hints); `WidgetConfig.tsx` page at `/widget-config/:instanceId` — loads via `GET /api/widget-config/:id`, saves via `PUT`, live mirror update via `store.onDidAnyChange` → `config:changed` IPC; gear ⚙ icon on Layout Editor widget cards (shown only when schema has fields) → navigates to config page                                                                                                                                                                   |
| 9   | Companion API — Full REST             | ✅ Done | `GET /api/system/info` (uptime, mem, CPU, platform, versions); `api.delete` added to companion wrapper; `NotificationsPage.tsx` — send typed notification overlays to mirror; `HomePage.tsx` — system info panel with memory bar; `App.tsx` — `/notifications` route + "Notify" nav link                                                                                                                                                                                                                                                                                                                                                           |
| 10  | Widget: Weather                       | ✅ Done | OpenWeather One Call 3.0; configSchema: apiKey/lat/lon/units; no-key + loading + error states; 3-day forecast strip                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 11  | Widget: Calendar                      | ✅ Done | `ical.js`; IPC proxy for CORS-free iCal fetch; Google Calendar secret iCal URL; next 5 events with date/time; recurring event expansion; 30-min refresh                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 12  | Widget: News / Headlines              | ✅ Done | GNews `/v4/top-headlines`; IPC proxy; config: `apiKey`, `country`, `category`, `lang`; auto-cycles headlines every 12 s with fade; 15-min refresh; switched from NewsAPI (no India support) to GNews (supports `country=in`, `lang=hi` etc.)                                                                                                                                                                                                                                                                                                                                                                                                       |
| 13  | Widget: Spotify / Now Playing         | ✅ Done | Spotify OAuth 2.0 Authorization Code flow; companion server `/spotify/auth` (redirect to Spotify) + `/spotify/callback` (exchange code, save tokens); `spotify:now-playing` IPC proxy — fetches `/v1/me/player/currently-playing`, auto-refreshes expired token; widget polls every 5 s, shows album art + track + artist + album + progress bar (500 ms interpolation); CSRF protection via `crypto.randomBytes(16)` state with 10-min expiry; `accessToken`/`refreshToken` stored in config (not in configSchema — OAuth-managed); `WidgetConfig.tsx` Connect Spotify section with redirect URI hint                                             |
| 14  | Widget: Stocks / Crypto               | ✅ Done | CoinGecko `/api/v3/coins/markets`; direct `fetch()` from renderer (CoinGecko supports CORS); config: `coins` (comma-sep CoinGecko IDs, up to 12), `currency` (usd/eur/gbp/inr/jpy/cad/aud), optional `apiKey` (demo key — adds `x-cg-demo-api-key` header); price formatted by magnitude (M/K/>1000/>1/sub-$1); 24h % change green▲/red▼; user coin order preserved; 5-min refresh; no API key required for free public tier                                                                                                                                                                                                                       |
| 15  | Dynamic Widget Loader                 | ✅ Done | `registerSchemesAsPrivileged` + `protocol.handle('widget', ...)` in `widget-protocol.ts`; serves `<userData>/widgets/<id>/<version>/bundle.esm.js` via `net.fetch`; path-traversal guard; dev-mode auto-seeds `test-widget/0.1.0/bundle.esm.js`; `useWidget(id, version)` hook — bundles resolve synchronously (no flash), dynamic widgets import from `widget://<id>/<version>/bundle.esm.js` with module-level cache; `React` set on `globalThis` in `main.tsx` for dynamic bundles; `WidgetWrapper` now uses `useWidget` — bundled widgets unaffected, unknown IDs fall back to loading skeleton then error state                               |
| 16  | Widget Registry (online)              | ✅ Done | `widget-registry.service.ts` — SSRF-safe fetch, 1-hr cache in `registry-cache.json`, `listWidgets()` / `installWidget()` / `uninstallWidget()`; `GET /api/widgets/registry` → `{ available, installed }`; `POST /api/widgets/install` downloads + saves bundle to `userData/widgets/<id>/<version>/`; `DELETE /api/widgets/:id` also removes from disk; `WidgetStore.tsx` companion page — card grid, install/uninstall buttons, update badge, empty state when no registry URL configured; `registryUrl` in `AppConfig.settings`; `InstalledWidget` gains `name` field; `fullCatalog` in `LayoutEditor` merges static + installed dynamic widgets |
| 17  | Automation Service                    | ⬜      | `node-cron`, brightness schedule, wake/sleep                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 18  | Polish                                | ⬜      | Transitions, night mode, user profiles                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

---

## Key Decisions Made

| Topic                | Decision                                                                                  |
| -------------------- | ----------------------------------------------------------------------------------------- |
| Background           | Always pure `#000000` — mirror glass constraint. See `docs/UI_DESIGN_RULES.md`            |
| Text                 | Always white / `text-white/[opacity]`. No slate/gray backgrounds on mirror UI             |
| QR code colours      | `dark: #000000`, `light: #ffffff` — functional requirement                                |
| Page navigation      | SWIPE_LEFT = next (content slides left). Clamped, no wrap-around                          |
| Boot page            | Always page 0 on startup (no persistence of last active page)                             |
| Page dots            | Always visible, bottom-centre                                                             |
| Arrow keys           | Dev fallback for gestures (←→↑↓ map to SWIPE_LEFT/RIGHT/UP/DOWN)                          |
| WiFi non-Linux       | `wifi.configured` auto-set `true` on non-Linux — app skips setup screen in dev            |
| Default pages        | Home / Media / Info seeded in main process on first run                                   |
| Widget grid          | 12 columns × 8 rows CSS grid, layout stored as `{ col, row, colSpan, rowSpan }`           |
| `ignoreDeprecations` | Must stay `"6.0"` in `tsconfig.web.json` — do not change                                  |
| Typecheck web        | Run as `npx tsc --noEmit -p tsconfig.web.json --composite false --ignoreDeprecations 5.0` |

---

## File Map (what was added/changed per phase)

### Main Process (`src/main/`)

| File                                   | Phase         | Notes                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `config/app-config.ts`                 | P0→P16        | `AppConfig`, `Page`, `WidgetInstance`, `InstalledWidget` types (P16: added `name` field); `electron-store`; `getConfig`, `setConfig`, `onConfigChange`; P16: `registryUrl: string` in settings defaults                                                                                                                                                                 |
| `services/wifi.service.ts`             | P1            | `isConnected`, `startHotspot`, `stopHotspot`, `scanNetworks`, `connectToNetwork`                                                                                                                                                                                                                                                                                        |
| `services/companion-server.service.ts` | P1–P16        | Full REST API + WebSocket; P13: Spotify OAuth routes (`/spotify/auth`, `/spotify/callback`), CSRF state, token exchange; P16: `registryService` setter; `GET /api/layouts` returns `installedWidgets`; `GET /api/widgets/registry` → `{ available, installed }`; `POST /api/widgets/install`; `DELETE /api/widgets/:id` also calls `uninstallWidget`                    |
| `services/widget-registry.service.ts`  | P16           | `WidgetRegistryService` — SSRF-safe `fetchUrl`; 1-hr memory+disk cache (`userData/registry-cache.json`); `listWidgets()` fetches GitHub manifest; `installWidget` downloads+sanitizes+writes bundle; `uninstallWidget` removes from disk + config                                                                                                                       |
| `services/websocket.service.ts`        | P5 fixup + P6 | `GestureWebSocketClient` — WS client connecting to Python gesture service (`ws://localhost:5001`); exponential backoff reconnect; `onGesture` callback + `connected` getter added in P6                                                                                                                                                                                 |
| `services/companion-server.service.ts` | P1–P6         | Added static SPA serving from `out/companion/` with SPA fallback + MIME types; `setGestureStatusProvider()`; `/api/status` now returns real gesture connection state                                                                                                                                                                                                    |
| `services/setup-server.service.ts`     | —             | Old — superseded by `companion-server.service.ts`, kept for reference                                                                                                                                                                                                                                                                                                   |
| `config/ipc.config.ts`                 | P3 cleanup    | Cleared — gesture IPC to be added in P5                                                                                                                                                                                                                                                                                                                                 |
| `index.ts`                             | P0–P16        | Seeds pages + widget instances; IPC handlers for `config`, `system:stats`, `page:changed`, `companion:url`, `calendar:fetch-ical`, `news:fetch-headlines`, `spotify:now-playing`; `fetchUrlText` SSRF utility; P15: `registerWidgetScheme` + `handleWidgetProtocol`; P16: `WidgetRegistryService` instantiation + `companionServer.setRegistryService(registryService)` |

### Preload (`src/preload/`)

| File         | Phase  | Notes                                                                                                                                                                                                |
| ------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.ts`   | P0–P11 | `window.config.get/set/onChange`; `window.api.onGesture`, `onWifiConnected`, `notifyPageChange` (P5); `window.system.getStats` (P4); `window.api.getCompanionUrl` (P7); `window.api.fetchIcal` (P11) |
| `index.d.ts` | P0–P11 | Types for `window.config`, `window.api` (incl. `notifyPageChange`, `getCompanionUrl`, `fetchIcal`), `window.system`                                                                                  |

### Renderer (`src/renderer/src/`)

| File                              | Phase   | Notes                                                                                                                                                                                                                       |
| --------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `App.tsx`                         | P1      | Routes to `SetupPage` or `MirrorPage` based on `wifi.configured`                                                                                                                                                            |
| `pages/SetupPage.tsx`             | P1      | QR code, AP instructions, black bg, white text                                                                                                                                                                              |
| `pages/MirrorPage.tsx`            | P2      | Renders `<PageSystem />`                                                                                                                                                                                                    |
| `hooks/useGesture.ts`             | P2      | Typed gesture hook + arrow-key fallback                                                                                                                                                                                     |
| `hooks/useConfig.ts`              | P2      | Reactive config slice hook                                                                                                                                                                                                  |
| `ui/PageSystem/PageSystem.tsx`    | P2–P7   | Slide animation, gesture nav, renders `WidgetGrid` per page; notifies page change via IPC (P5); `<CompanionQR />` overlay (P7)                                                                                              |
| `ui/PageSystem/PageDots.tsx`      | P2      | Active dot = pill, inactive = small; always visible                                                                                                                                                                         |
| `ui/WidgetGrid/WidgetGrid.tsx`    | P3      | 12×8 CSS grid, maps `widgetIds` → `WidgetWrapper`                                                                                                                                                                           |
| `ui/WidgetGrid/WidgetWrapper.tsx` | P3      | `ErrorBoundary` + grid placement via CSS `gridColumn/Row`                                                                                                                                                                   |
| `lib/widget-types.ts`             | P3      | `WidgetProps`, `WidgetManifest`                                                                                                                                                                                             |
| `lib/widget-registry.ts`          | P3–P14  | `BUNDLED_WIDGETS` map; added `clock`, `system-stats`, `weather`, `calendar`, `news`, `spotify`, `stocks`                                                                                                                    |
| `widgets/dummy/DummyWidget.tsx`   | P3      | Test widget — renders instance ID                                                                                                                                                                                           |
| `widgets/clock/index.tsx`         | P4      | Live clock; `setInterval` 1 s; config: `timezone`, `use24h`                                                                                                                                                                 |
| `widgets/system-stats/index.tsx`  | P4      | CPU/RAM/temp bars; polls `window.system.getStats()` every 3 s                                                                                                                                                               |
| `widgets/weather/index.tsx`       | P10     | OpenWeather One Call 3.0 fetch; config: `apiKey`, `lat`, `lon`, `units`; current temp + conditions + 3-day forecast; 10-min poll                                                                                            |
| `widgets/calendar/index.tsx`      | P11     | `ical.js` parser; fetched via `window.api.fetchIcal` IPC proxy; config: `icalUrl`; next 5 events; recurring expansion; 30-min poll                                                                                          |
| `widgets/news/index.tsx`          | P12     | GNews `/v4/top-headlines`; fetched via `window.api.fetchHeadlines` IPC proxy; config: `apiKey`, `country`, `category`, `lang`; auto-cycles 12 s fade; 15-min refresh                                                        |
| `widgets/spotify/index.tsx`       | P13     | Polls `window.api.spotifyNowPlaying(instanceId)` every 5 s; album art (`<img>` direct URL), track name/artist/album, progress bar with 500 ms client-side interpolation; states: loading/no-token/idle/error/playing/paused |
| `widgets/stocks/index.tsx`        | P14     | CoinGecko `/api/v3/coins/markets`; direct `fetch()` (CoinGecko supports CORS); config: `coins`, `currency`, optional `apiKey`; price formatted by magnitude; 24h % change green▲/red▼; 5-min refresh                        |
| `hooks/useWidget.ts`              | P15     | `useWidget(id, version)` — resolves bundled widget synchronously, else dynamic-imports from `widget://<id>/<version>/bundle.esm.js`; module-level cache; loading / error states                                             |
| `ui/WidgetGrid/WidgetWrapper.tsx` | P3→P15  | Updated: now calls `useWidget` hook instead of direct `BUNDLED_WIDGETS` lookup; handles loading skeleton + error state for unknown/dynamic widget IDs                                                                       |
| `components/ConnectionStatus.tsx` | cleanup | Null stub — original deleted                                                                                                                                                                                                |
| `components/UI.tsx`               | cleanup | Null stub — original deleted                                                                                                                                                                                                |
| `App.tsx`                         | P6      | Added `config.onChange` theme subscription — sets `data-theme` on `<html>` in real-time                                                                                                                                     |
| `components/CompanionQR.tsx`      | P7      | QR code overlay (bottom-right mirror corner); calls `getCompanionUrl` IPC; tap to toggle hide                                                                                                                               |

### Companion (`src/companion/`)

| File                                  | Phase  | Notes                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.html`                          | P6     | SPA entry point                                                                                                                                                                                                                                                                                                                        |
| `src/main.tsx`                        | P6→P15 | React DOM mount; P15: sets `globalThis.React` for dynamic widget bundles                                                                                                                                                                                                                                                               |
| `src/index.css`                       | P6     | `@import "tailwindcss"` via `@tailwindcss/vite`                                                                                                                                                                                                                                                                                        |
| `src/App.tsx`                         | P6–P16 | `BrowserRouter` + sticky nav; routes: `/`, `/settings`, `/wifi`, `/layout`, `/widget-config/:instanceId`, `/notifications`; P16: added `/store` route + "Store" nav link                                                                                                                                                               |
| `src/lib/api.ts`                      | P6–P9  | `api.get/put/post/delete` typed fetch wrappers; relative paths (served from same origin as API)                                                                                                                                                                                                                                        |
| `src/lib/themes.ts`                   | P6     | `THEMES` constant array + `ThemeValue` type — shared source of truth for theme options                                                                                                                                                                                                                                                 |
| `src/lib/widgets-catalog.ts`          | P7–P14 | `WIDGET_CATALOG` (`CatalogEntry[]`); weather: `apiKey`/`lat`/`lon`/`units`; calendar: `icalUrl`; news: `apiKey`/`country`/`category`/`lang`; spotify: `clientId`/`clientSecret` (tokens not in schema); stocks: `coins`/`currency`/`apiKey`                                                                                            |
| `src/pages/HomePage.tsx`              | P6–P9  | Polls `/api/status` every 2 s; `/api/system/info` on mount — uptime, memory bar, CPU, platform; WS gesture + page events                                                                                                                                                                                                               |
| `src/pages/NotificationsPage.tsx`     | P9     | Send typed notification (info/success/warning/error) via `POST /api/notifications`; session history list                                                                                                                                                                                                                               |
| `src/pages/SystemSettings.tsx`        | P6→P16 | Full settings form: theme dropdown, wake timeout, brightness sliders, schedule pickers → `PUT /api/settings`; P16: added `registryUrl` URL input                                                                                                                                                                                       |
| `src/pages/WifiSetupPage.tsx`         | P6     | Network scan list + password field → `POST /api/wifi/connect`                                                                                                                                                                                                                                                                          |
| `src/pages/LayoutEditor.tsx`          | P7–P16 | Drag/resize grid editor; gear ⚙ on configurable widgets; save → `PUT /api/layouts`; P16: `installedWidgets` state from layout response; `fullCatalog` (useMemo) merges static + installed dynamic widgets                                                                                                                             |
| `src/pages/WidgetStore.tsx`           | P16    | Widget store — `GET /api/widgets/registry`; card grid with install/uninstall buttons, loading states, update badge; empty state with Settings link when no registry URL configured                                                                                                                                                     |
| `src/pages/WidgetConfig.tsx`          | P8–P13 | `/widget-config/:instanceId` — loads instance via `GET /api/widget-config/:id` (falls back to React Router location state for new unsaved instances); saves via `PUT` (upsert); P13: added Spotify OAuth section — Connect button links to `/spotify/auth`, shows redirect URI hint, reflects OAuth result via `?spotify=` query param |
| `src/components/WidgetConfigForm.tsx` | P8     | Auto-renders form from `configSchema`: string/number/boolean toggle/select, secret masking, per-field hints                                                                                                                                                                                                                            |

### Build / Config

| File                       | Phase | Notes                                                                       |
| -------------------------- | ----- | --------------------------------------------------------------------------- |
| `companion.vite.config.ts` | P6    | Vite config for companion SPA; root `src/companion`, outDir `out/companion` |
| `package.json`             | P7    | Added `react-grid-layout` v2 dependency                                     |

---

## Dev Workflow Notes

- `npm run dev` — starts Electron with HMR
- `npm run typecheck:node` — check main + preload
- `npx tsc --noEmit -p tsconfig.web.json --composite false --ignoreDeprecations 5.0` — check renderer
- To force WiFi setup screen: `await window.config.set('wifi.configured', false)` in DevTools, reload
- To reset widget state: `await window.config.set('widgetInstances', {})`, `await window.config.set('pages', [])`, reload
- `npm run build:companion` — build companion SPA to `out/companion/`
- `npm run dev:companion` — watch-build companion SPA alongside `npm run dev`
- Gesture service runs independently; Electron connects to it at `ws://localhost:5001` (Python default port in `gesture_service/src/backend/config.py`)
- Layout editor limits: max **8 widgets/page**, max **5 pages** (constants `MAX_WIDGETS_PER_PAGE`, `MAX_PAGES` in `LayoutEditor.tsx`)
- **Building a new widget?** Follow `docs/WIDGET_AUTHORING.md` — covers component contract, configSchema, registry registration, seeding, and end-to-end checklist

---

| Gesture WS client | `GestureWebSocketClient` in `websocket.service.ts`; connects to `ws://localhost:5001`; single `close` handler drives reconnect; no state broadcasts to renderer |

- Register both in `BUNDLED_WIDGETS`
- Seed them as real instances replacing the dummy widget
- Both must follow `UI_DESIGN_RULES.md` — black bg, white text
